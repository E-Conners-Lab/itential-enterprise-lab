# ansible/ — guest OS and application configuration

All OS-level and application configuration for every VM and container
(tac_plus, Oxidized, Kea/BIND9, Windows via WinRM, Itential OS prep, ...).
Inventory is generated from `topology/` + NetBox, never hand-maintained.
