# Handoff — Phase 7 (observability) close-out (state after the 2026-09-10 session)

Paste the block below into a new Claude Code session opened in this repo (the `itential-builder` plugin is
installed at user scope).

---

Continue the itential-enterprise-lab project (repo https://github.com/E-Conners-Lab/itential-enterprise-lab,
local ~/PycharmProjects/itential-enterprise-lab). Phases 0-6 are merged; Phase 7 (observability, S7) is on
`phase-7/observability` with its PR open. Read this file, then `docs/PID.md` (S7 with amendments 1.15/1.16,
section 3), ADR 0050 and 0051, `k8s/observability/versions.yaml`, `observability/observability.yaml`,
`observability/expiries.yaml`, `ansible/playbooks/observability*.yml`, `verify/test-07-observability.sh` and
`tests/test_observability.py`. Memory in
`~/.claude/projects/-Users-elliotconner-PycharmProjects-itential-enterprise-lab/memory/` holds the measured
shapes and the lessons of this phase (`lab-build-lessons.md`, Phase 7 entry).

## Rules (unchanged)
One branch and one PR per phase; the PR description is the phase report (built / verified / deferred).
Conventional Commits; secrets only in `.env`; every decision an ADR (next 0052); PID amendment (next 1.17)
before code when the design changes. TDD: red tests and a red verify check first. One element per commit with
pytest, ansible-lint (140-character lines) and the verify log. Everything through APIs from documents in the
repo, idempotent. Device writes only through `wf-config-push-v1` with a Work Center approval; ask before anything
destructive or touching vmbr0. Post a detailed change summary and wait for Elliot's verbal "approve" before
merging. Never fabricate a "done". `verify/devcmd.py` is the independent second source. Budget (ADR 0049): the
Anthropic key is the company key, $15 a week; `make tokens` before any agent verify. `kubectl get pods -A` after
k3s changes (kubeconfig `~/.kube/lab-k3s.yaml`).

## State
- Stack in namespace `observability` (ADR 0051): Zabbix 7.0.30 on CNPG `zabbix-db`, kube-prometheus-stack
  (release `kps`), SNMP + blackbox exporters, gNMIc (vEOS), Loki single-binary, `alloy-logs` DaemonSet,
  `alloy-syslog` receiver on `.38`, the Platform exporter and the Platform's own `/prometheus_metrics` route.
  UIs on the IP plan's VIPs through Traefik: zabbix .35, grafana .36, prometheus/alertmanager .37, loki .38,
  gnmic .39 (`*.lab.internal`, lab CA). Passwords: `ZABBIX_ADMIN_PASSWORD`, `GRAFANA_ADMIN_PASSWORD`,
  `ZABBIX_GRAFANA_PASSWORD`, `SNMPV3_AUTH_PASSWORD`, `SNMPV3_PRIV_PASSWORD` in `.env` (generated by the play).
- Zabbix: 24 hosts from NetBox (+ eve, netbox), `lab-uis` (9 HTTP checks), `expiries` (5 Script items, 14-day
  triggers); templates in `observability/zabbix/templates/` (two generated by `zabbix/build.py`).
- Every router and switch carries SNMPv3 (user `zabbix`), syslog to `.38`, EOS gNMI in VRF MGMT and IOS XE
  `logging origin-id hostname`, pushed through `wf-config-push-v1` (Elliot approved 24 + 5 cards; 7 no-op vEOS
  cards were cancelled through the API). Golden Config baselines carry the same lines.
- Verify: `verify/results/20260910T024019Z-07-observability.log` 7 PASS / 1 DEFER (S7.5 -> phase 9),
  `20260910T024307Z` the S7.6 drill (trigger + alert 68 s after the stop, cleared 257 s after).
- Agents: Zabbix agent 2 7.0.30 + rsyslog forward on all 12 Ubuntu machines (`observability-hosts.yml`).

## Open items
- **br1-wan01 after the drill (resolved 2026-09-10 ~03:10 UTC)**: the hard stop showed its "Next reload license
  Level" was empty (the other four routers have `network-advantage` + `dna-advantage`), so it booted without
  crypto. Two governed pushes (licence boot level, job e9709d5667344996be51fd6b; then the IKEv2/IPsec block
  rendered from `c8000v.j2`, job 566d3b4346b7424bad815e8e, because the crypto-less boot plus `write memory`
  had dropped it from the startup config) and one reload restored it: 2 IKEv2 SAs READY, both tunnel BGP
  sessions Established, DC edge pings the branch gateway. Lesson: a C8000v whose next-reload level is empty
  loses every crypto line on the first reload; check `show version | include Next reload` on every router
  before any reload drill.
- Issue #18: the `{#-` after `hostname {{ name }}` in `topology/configs/c8000v.j2` swallowed the newline
  (fixed in this phase, reference configs regenerated); the NTP half of the issue is still open.
- `docs/ip-plan.md` section 6 regeneration from the YAML (ADR 0048), the Gateway runner's `NETBOX_URL`
  (home-LAN value), `netbox-seed.yml` now deletes `dc01`/`iag` on its next run (`make phase-observability`
  runs it; not yet run in this session).
- Warts: `community.zabbix` template import reports "changed" for three templates on every run; the Alloy
  config reloader needed a rollout restart to load a ConfigMap change; Prometheus/Alertmanager UIs have no
  login (Keycloak front is a phase-9 candidate with S7.5); Grafana dashboards "Firewalls" waits for the
  firewall track.

## Phase 7 follow-up (ADR 0052, branch `phase-7/itential-dashboard`)
The official Itential Platform Monitoring dashboard (grafana.com 25527) is vendored and provisioned; node/process/
Redis/MongoDB exporters run beside the dev-stack (Compose profile `monitoring`, `itential/compose.override.yml`);
the lab exporter supplies the `itential_job_*`/`itential_task_*` series; S7.8 verifies it. MongoDB is standalone
in the dev-stack, so the dashboard's replica-set panels stay empty by design.

## Next phase
Phase 8 config/secrets/code (`phase-8/config-secrets-code`, S8): Oxidized, Vault (takes the device credentials,
the SNMPv3 passphrases and the company key), Gitea.
