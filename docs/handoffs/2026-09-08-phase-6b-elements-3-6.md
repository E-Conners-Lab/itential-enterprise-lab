# Handoff prompt — Phase 6b, S4d elements 3 to 6 (Lifecycle Manager, Integration Models, agent fleet, hosts)

Paste the block below into a new Claude Code session opened in this repo (the `itential-builder`
plugin is installed at user scope and loads at session start).

---

Continue the itential-enterprise-lab project (repo https://github.com/E-Conners-Lab/itential-enterprise-lab,
local ~/PycharmProjects/itential-enterprise-lab, branch `phase-6/flowai`, last commit adce2a6).
Read `docs/PID.md` (S4c and S4d), ADR 0037-0042, `itential/versions.yaml`, `ansible/playbooks/itential.yml`,
`ansible/playbooks/platform.yml` with `ansible/playbooks/tasks/golden-config*.yml` and `tasks/mop.yml`,
`ansible/playbooks/flowai.yml` with `tasks/flowai-agent.yml`, `itential/workflows/build.py`,
`itential/agents/*.yaml`, `verify/test-06b-platform.sh`, `verify/test-06-flowai.sh`,
`tests/test_golden_config.py` and `tests/test_mop.py` before writing anything. Memory notes in
`~/.claude/projects/-Users-elliotconner-PycharmProjects-itential-enterprise-lab/memory/` hold the API lessons
(itential-platform-lessons.md has the Configuration Manager, Operations Manager and MOP shapes measured on 6.5.2).

## Rules (unchanged)
- One branch and one PR per phase; the PR description is the phase report (built / verified / deferred).
  This work continues on `phase-6/flowai` (rebase onto main after PR #17 merges).
- Conventional Commits; secrets only in `.env` (gitignored); every decision is an ADR in `docs/adr/`
  (next number 0043); amend the PID (next amendment 1.9) before code when the design changes.
- TDD: red unit tests and a red verify check first, then the implementation, then green. One element
  per commit; `pytest -q` (94 today), ansible-lint (pre-commit enforces a 140-character line limit) and
  the verify log go in the commit.
- Everything is created through the Platform API from documents in the repo and re-runs idempotently:
  the pattern is `platform.yml` + `tasks/<element>.yml` reading a `versions.yaml` section and documents
  under `itential/<element>/`. Nothing is clicked into the UI. Play order after any workflow change:
  `itential.yml` (re-imports every `wf-*`, new uuids) -> `platform.yml` (re-points automations by PATCH)
  -> `flowai.yml` (re-resolves agent tools). `make phase-flowai` runs the three. Export
  `NETBOX_API=$NETBOX_URL` when running plays by hand.
- Device writes go only through `wf-config-push-v1` (Work Center approval). The auto-mode classifier
  blocks Claude finishing Work Center tasks for device pushes outside the verify scripts: start the jobs,
  report the ids, and ask Elliot to approve the cards; the verify scripts finish their own approvals.
- Ask in chat before anything destructive or touching vmbr0. Post a detailed change summary and wait for
  a verbal "approve" before merging. Never fabricate a "done". `verify/devcmd.py <ip> "<cmd>"` is the
  independent second source for every device check (drop the EOS echo line `> <cmd>`).
- Use the itential-builder plugin skills as the reference for object shapes (`/itential-builder:itential-lcm`,
  `itential-json-forms`, `flowagent`, `itential-inventory`, `explore`) and the helper assets under
  `~/.claude/plugins/cache/itential-builder/itential-builder/1.5.0/helpers/`; measure any shape the skill
  documents before trusting it (on 6.5.2 the golden-config and MOP skills were wrong in four places,
  ADR 0040/0042) and record what you measure in memory.

## Lab facts (state after adce2a6)
- Platform https://itential.lab.internal (VM 205, 10.100.0.65; lab CA `docs/lab-root-ca.crt`; login
  `admin@itential` / `ITENTIAL_ADMIN_PASSWORD` from `.env`). Gateway 5 cluster `lab` with the glibc runner
  (ADR 0038). Inventory Manager inventory `lab` = 12 network nodes from NetBox (5 IOS-XE `cisco_ios`,
  7 EOS `arista_eos`); Configuration Manager reads them through the `InventoryBroker` adapter (ADR 0039).
- Element 1 (ADR 0040/0041): device groups `site-*` and `role-*`; Golden Config trees `lab-cisco-ios` and
  `lab-arista-eos` (base/<site>/<device>, documents `itential/golden-config/<type>/`); compliance plan
  `lab-baseline` (12 nodes) run nightly at 03:00 UTC by `wf-compliance-run-v1`. All five C8000v routers
  had `hostname Router` and no `ntp server` line since Phase 4; both restored through
  `wf-config-push-v1` with Elliot's approvals; root cause = GitHub issue #18.
- Element 2 (ADR 0042): MOP command templates `lab-cisco-ios-checks` / `lab-arista-eos-checks` and
  analytic templates `lab-<type>-prepost` (documents `itential/command-templates/`); nightly backups at
  02:30 UTC by `wf-backup-all-v1` (forEach over Configuration Manager devices).
- Workflows: wf-netbox-device-count-v1, wf-show-version-v1, wf-show-command-v1, wf-branch-vlan-v1,
  wf-config-push-v1, wf-compliance-run-v1, wf-backup-all-v1 (all generated by build.py).
  Agents: lab-netops (Claude), lab-netops-local (qwen2.5:7b in-lab), lab-netops-mac (optional).
  MCP server http://mcp.lab.internal:8000/mcp hides `describe_inventory` and `get_devices` (they return
  the node password; ADR 0039 amendment). NetBox adapter `NetBox` (export `Netbox`); ServiceNow PDI
  dev409097 (adapter `ServiceNow`, user itential.integration, keep-alive login every 10 days).
- verify: `verify/test-06b-platform.sh` S4d.1 and S4d.2 PASS (20260908T002153Z), S4d.3/4/6 print DEFER;
  `verify/test-06-flowai.sh` S4c.1-7 (S4c.2/4/6 re-run green after the last re-import).

## Scope: PID S4d elements 3 to 6, in this order, one element per commit
3. **Lifecycle Manager + JSON Forms** (S4d.3): resource model `branch-vlan` (documents in
   `itential/lcm/`) with actions create -> `wf-branch-vlan-v1` and delete -> a new `wf-branch-vlan-delete-v1`
   from build.py (NetBox VLAN deleted, `no vlan N` on the switch through the approval, no-op when absent);
   instances for the VLANs that exist; a JSON Form (`itential/forms/`) on the approval task showing branch,
   VLAN, switch and the NetBox reservation. Acceptance: create -> instance -> delete round trip with the
   Work Center approval; instance history recorded; verify S4d.3 finishes its own approvals.
4. **Integration Models** (S4d.4): ServiceNow (OpenAPI upload + instance with `itential.integration`,
   helpers/assets/openapi-specs) and NetBox, registered as agent tools. Acceptance: an agent reads an
   incident and a NetBox device through the integration models (session shows the integration tool).
5. **Agent fleet** (S4d.5, memory `work-laptop-agent-fleet`): netbox-sot, device-ops, compliance,
   diagnostics, remediation as `itential/agents/*.yaml`, each on Claude and a local twin; tiered autonomy;
   compliance uses the plan/report tools, remediation's only write tool is `wf-config-push-v1`.
   Acceptance per agent in `verify/test-06-flowai.sh` with token usage.
6. **Hosts and firewalls** (S4d.6): the three Ubuntu hosts as inventory nodes (`linux`, reachability and
   uptime); PA-VM firewalls stay deferred until the image is staged.
Then: full runs of test-06 and test-06b, open the Phase 6 draft PR (description = phase report, ADRs
0037-004x, verify log paths), rebase onto main once PR #17 merges, and wait for the verbal approval.
Deferred, do not build: Gateway 4, observability/job metrics (Phase 9), node-credential secrets (Phase 10),
Windows endpoints.

## First step
Confirm the gate (clean git status on `phase-6/flowai` at adce2a6, `pytest -q` 94 passing, Platform
health answers, the 20260908T002153Z 06b log shows passed=2), then propose the ADR list and the
document layout for element 3 before writing code.

Calendar: the Phase 5 PR #17 memory check is due after 2026-09-08 about 19:10 UTC; the Phase 6 draft
PR still needs opening once PR #17 merges; ServiceNow PDI interactive login every 10 days.
