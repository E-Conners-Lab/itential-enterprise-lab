# k8s/ — Helm values and Kustomize overlays

No k3s cluster exists yet (see `docs/discovery.md`). Cluster build and the
observability / config / secrets services land here per the PID phases.
Image tags are pinned; `:latest` is not allowed.
